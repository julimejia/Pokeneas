<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= name %> - Pokenea</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');
      .pokenea-font {
        font-family: 'Press Start 2P', cursive;
      }
      .pokenea-image {
        background: linear-gradient(45deg, #f3f4f6, #e5e7eb);
      }
    </style>
  </head>
  <body class="bg-gray-900 font-sans">
    <!-- Header con nombre del Pokenea -->
    <header class="w-full py-6 bg-gradient-to-r from-yellow-400 via-red-500 to-blue-500 text-white text-center text-3xl font-bold pokenea-font shadow-lg border-b-4 border-black">
      <%= name %>
    </header>

    <!-- Main layout -->
    <div class="flex h-[calc(100vh-88px)]">
      <!-- Sidebar -->
      <aside class="w-20 bg-gradient-to-b from-yellow-400 via-red-500 to-blue-500 flex flex-col items-center py-8 space-y-6 border-r-4 border-black">
        <div class="w-12 h-12 bg-white rounded-full border-4 border-black flex items-center justify-center">
          <div class="w-6 h-6 bg-red-500 rounded-full border-2 border-black"></div>
        </div>
      </aside>

      <!-- Main content -->
      <main class="flex-1 flex flex-col justify-center items-center p-8 space-y-6">
        <div class="relative w-[320px] h-[420px] bg-white shadow-2xl rounded-xl overflow-hidden border-4 border-black">
          <!-- Imagen -->
          <div class="pokenea-image w-full h-full">
            <img src= <%= imageUrl %> alt="<%= name %>" 
                 class="w-full h-full object-contain p-4" />
          </div>

          <!-- Overlay superior -->
          <div class="absolute top-4 left-4 bg-white bg-opacity-90 px-4 py-2 rounded-lg shadow-md border-2 border-black">
            <p class="text-sm text-gray-800 font-bold pokenea-font tracking-wide">CONTAINER</p>
          </div>

          <!-- Overlay inferior -->
          <div class="absolute bottom-4 left-0 right-0 mx-auto bg-white bg-opacity-90 px-4 py-3 rounded-lg shadow-md border-2 border-black w-3/4">
            <p class="text-xs text-gray-800 text-center pokenea-font leading-tight">
              "<%= frase %>"
            </p>
            <p class="text-xxs text-gray-600 text-center mt-1">Container ID: <%= containerId %></p>
          </div>
        </div>

        <!-- Botones -->
        <button class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-full border-4 border-black transition-all transform hover:scale-105 pokenea-font text-sm"
                onclick="window.location.href='/'">
          VOLVER
        </button>

        <button class="bg-yellow-500 hover:bg-yellow-600 text-white font-bold py-3 px-8 rounded-full border-4 border-black transition-all transform hover:scale-105 pokenea-font text-sm"
                onclick="window.location.reload()">
          VER OTRO
        </button>
      </main>

      <!-- Sidebar derecha -->
      <aside class="w-24 bg-gradient-to-b from-blue-500 via-purple-500 to-pink-500 flex flex-col items-center justify-center py-8 space-y-4 border-l-4 border-black">
        <div class="text-white text-xs pokenea-font rotate-90 mt-8 whitespace-nowrap tracking-wider">
          POKENEA
        </div>
      </aside>
    </div>
  </body>
</html>
